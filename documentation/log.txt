Project: Do Tech Layoffs Help Stock Prices?
Course: CS 210 — Data Management for Data Science
Phase: 1 of 5 (Database Architecture & Data Ingestion)
Status: Complete

What We Built
In Phase 1, we set up the foundation for the entire project: a database to store our data, and a Python script to load the layoffs dataset into it. Everything after this phase — stock prices, analysis, visualizations — depends on this foundation being solid.
Think of Phase 1 as pouring the concrete slab before building the house. It's not glamorous, but if it's crooked, everything built on top of it will be crooked too.

The Big Picture: What Problem We Solved
We started with a CSV file of layoff events downloaded from Kaggle (originally scraped from layoffs.fyi). That file has about 4,300 rows of messy, inconsistent data — some rows have missing values, some companies appear multiple times with slight spelling differences, some dates are unparseable, and percentages are stored as text with "%" signs.
We needed to turn that mess into a clean, queryable database that the rest of our pipeline can trust. Phase 1 accomplishes exactly that.

Step-by-Step: What We Did
1. Designed the Database Schema
We created a PostgreSQL database with five tables, each with a specific job:
TableWhat It StoresWhy It ExistscompaniesOne row per company (name, industry, HQ, ticker)The "master list" that everything else links back tolayoff_eventsOne row per layoff announcementThe core event data from layoffs.fyidaily_pricesOne row per (company, trading day) stock priceWill hold ~30K rows of stock data in Phase 3market_indexS&P 500 daily pricesOur benchmark for computing "abnormal returns"event_windowsComputed analytical results per eventOutput of our analysis in Phase 4
Key design principle: each table does one thing. We avoided the beginner mistake of cramming everything into one giant table.
2. Enforced Data Quality at the Database Level
The database itself will reject bad data before it ever gets stored. We added:

Primary keys (SERIAL auto-incrementing IDs) so every row has a unique identifier
Foreign keys so you can't create a layoff event for a company that doesn't exist
UNIQUE constraints so you can't have two companies named "Meta" or two stock prices for the same company on the same day
CHECK constraints that reject impossible values (negative employee counts, percentages over 100%, negative stock prices)
Indexes on the columns we'll query most often, so lookups are fast

3. Built the Ingestion Pipeline
We wrote ingest_layoffs.py, a Python script that:

Connects to PostgreSQL using credentials stored in a .env file (never hardcoded)
Loads the layoffs CSV into a pandas DataFrame
Cleans the data:

Standardizes column names to snake_case
Strips whitespace from text fields ("Meta " vs "Meta" would otherwise be two companies)
Parses dates properly, skipping rows with unparseable dates
Converts text numbers ("12%") to real numbers (12.0)
Removes duplicate rows, keeping the one with the most complete data


Inserts the data in two passes:

Pass 1: Insert unique companies
Pass 2: Insert layoff events, linking each to the correct company


Logs everything — every insert, skip, and error — to a log file we can review later

4. Made the Pipeline Safe to Re-Run
Every database insert uses ON CONFLICT DO NOTHING, which means if you run the script twice, it won't create duplicates. You can re-run ingestion as many times as you want during development, and the database stays clean. This property is called idempotency, and it's a big deal in data engineering.
5. Set Up the Project Structure
We established the repository layout:
layoffs-analysis/
├── sql/
│   └── schema.sql              # Database table definitions
├── data/
│   └── layoffs.csv             # Source data (not committed to git)
├── ingest_layoffs.py           # Phase 1 ingestion script
├── requirements.txt            # Python dependencies
├── .env                        # Database credentials (not committed to git)
├── .env.example                # Template for credentials
├── .gitignore                  # Tells git what to ignore
└── README.md                   # Project documentation

Key Decisions We Made (And Why)
These are the decisions you'll most likely be asked about in your oral exam. Each has a specific rationale behind it.
Why PostgreSQL instead of SQLite or MongoDB?
PostgreSQL handles the concurrency we'll need when we fetch stock prices in parallel (Phase 3). It also has stronger constraint enforcement than SQLite. MongoDB was ruled out because our data is inherently relational — events connect to companies which connect to prices — and a document database would force us to either duplicate data or do joins in Python, both of which are slow and error-prone.
Why Five Tables Instead of One?
This is called normalization (specifically, Third Normal Form / 3NF). If we stored everything in one giant table, we'd repeat Meta's industry, headquarters, and ticker symbol in every row of Meta's stock prices — that's 30,000 duplicate copies. If Meta's industry ever needed updating, we'd have to update all 30,000 rows and hope we didn't miss any. With normalized tables, Meta's info lives in one place. One row, one update, done.
Why SERIAL IDs Instead of Ticker Symbols as Primary Keys?
Tickers change over time. Meta used to be FB. Block used to be SQ. If we used ticker as the primary key, a rename would cascade through every table in our database. With stable numeric IDs, we can update ticker strings freely without breaking any relationships.
Why NUMERIC Instead of FLOAT for Prices?
Floating point numbers have small rounding errors built into how computers represent them — 0.1 + 0.2 doesn't exactly equal 0.3 in floating point. Over millions of calculations, these errors compound. NUMERIC gives exact decimal arithmetic. This is standard practice in any system that touches money, and it's the kind of detail that separates "student code" from "production code."
Why a Two-Pass Ingestion (Companies First, Then Events)?
Because of foreign key constraints, every layoff event must reference a company that already exists in the database. If we tried to insert both in one pass, we'd have to stop on every row to check whether the company exists yet. Two passes is cleaner: insert all unique companies first, then insert events that reference them. It also mirrors the logical dependency — events depend on companies, so companies come first.
Why Use errors="coerce" When Parsing Data?
The alternative is to let one bad row crash the entire ingestion. With coerce, pandas converts unparseable values to NaN (null), and our code handles those nulls explicitly. One malformed row out of 4,300 should not kill the pipeline. This is called graceful degradation, and it's one of the most important principles in data engineering.
Why Environment Variables for Database Credentials?
Hardcoding your password into a Python file is a serious security mistake — if you accidentally push that code to GitHub, your password is now public. We store credentials in a .env file that's listed in .gitignore, so it never gets committed. The .env.example file shows the format without containing any real secrets.

What We Learned About the Data
From the cleaning process, we confirmed several things about our dataset:

~4,300 layoff events spanning multiple years
~2,800 unique companies (some have done multiple rounds of layoffs)
Missing data is common: some rows have employee counts but not percentages, or vice versa. Our schema accommodates this by allowing NULLs in both fields.
Duplicate rows exist in the raw CSV, likely from the source's scraping process. Our deduplication step handles this.
Not all companies are public: a significant chunk are private startups with no ticker. These will be excluded from the stock analysis in Phase 3 but remain in the database for context.


====================================================================
PHASE 2a: TICKER MATCHING
====================================================================
Status: Complete
Date Completed: 2026-04-19

What This Phase Does
--------------------
Connects company names in our layoffs database to actual stock ticker
symbols (like META for Meta, AMZN for Amazon). This mapping is what
lets the next phase pull stock prices for each company from Yahoo
Finance. Without tickers, we have no way to look up price history.

Why This Phase Is Harder Than It Looks
--------------------------------------
Matching company names to tickers is a classic entity resolution
problem. Some examples of the mess:

  - "Meta" is easy - matches META directly.
  - "Google" is not - it trades under GOOGL (Class A), not GOOG.
  - "Apple" is dangerous - fuzzy-matches "Apple Hospitality REIT"
    (APLE) by string similarity, which would be completely wrong.
  - "Twitter" went private in 2022 - there is no ticker anymore.
  - "Stripe" has never been public - no ticker exists.
  - "Block" used to be "Square" and traded as SQ before renaming
    to XYZ in 2024.

There is no single algorithm that gets all of these right. The
professional answer is a layered strategy that tries the most
reliable method first and falls back to less reliable methods only
when needed.

Our Matching Strategy
---------------------
Three tiers, tried in order. First hit wins.

  Tier 1: Manual Override Map
    A hand-curated Python dictionary of about 130 entries mapping
    lowercase company name to ticker. Built by researching each of
    the top companies by layoff count. These are human-verified and
    get confidence score 100. The map also includes explicit "None"
    entries for known-private companies like Stripe and Databricks,
    which prevents the next tier from accidentally matching them to
    something wrong.

  Tier 2: yfinance Direct Validation
    For companies not in the manual map, we generate plausible ticker
    candidates from the name (first word, first few letters, acronym)
    and ask Yahoo Finance if any of them are real tickers. Critically,
    we do not accept just any response - we verify that the returned
    ticker's registered company name matches our input using fuzzy
    string comparison. This is what prevents "Apple" from matching
    AAP (Advance Auto Parts). Matches get confidence score 85.

  Tier 3: Unresolved
    Anything the first two tiers miss is marked as unresolved and
    excluded from stock analysis. Most of these are private startups
    that wouldn't appear in our analysis anyway.

What We Deliberately Did NOT Do
-------------------------------
We did not implement aggressive fuzzy matching against a big corpus
of ticker symbols. That would increase our match count but at the
cost of silent wrong matches. For an event study, a confident wrong
answer is much worse than an admitted gap - a wrong match pollutes
the analysis invisibly, while a gap is just a missing data point.
This is called the precision-over-recall tradeoff.

Schema Changes
--------------
Added three new columns to the companies table through a new
migration file (not by editing the original schema):

  match_method        - which tier matched this company (manual,
                        yf_direct, or unresolved)
  match_confidence    - 0 to 100, how sure we are about the match
  match_attempted_at  - timestamp of when matching ran

Also added two CHECK constraints so the database itself enforces that
if a method is set, confidence must also be set, and that method must
be one of the three valid values. Plus an index on match_method so
we can filter by match quality quickly.

Results
-------
Out of 2,870 total companies in the database:

  Matched to ticker:  89 companies (3%)
  Unresolved:         2,781 companies (97%)

At first glance, 3% looks terrible. But the key insight is that tech
layoffs follow a power-law distribution - a small number of huge
companies dominate the total impact. Here's the breakdown by total
employees laid off:

  Matched companies:    441,444 employees laid off (51% of total)
  Unresolved companies: 431,132 employees laid off (49% of total)

So we captured 51% of the actual layoff impact with just 3% of the
companies. Amazon alone accounts for about 27,000 layoffs - more
than thousands of small startup cuts combined. The unresolved
long tail is overwhelmingly private startups that wouldn't have
tradeable stock anyway.

Problems We Hit Along The Way
-----------------------------
1. Ticker collision crash
   Two different companies in the layoffs data were both matched to
   the same ticker "AUR" by the yfinance validator. The database's
   UNIQUE constraint on ticker_symbol correctly rejected the second
   one with an IntegrityError, but that crashed our whole script.
   We fixed this by catching that specific error in our code -
   when it fires, we demote the colliding company to unresolved
   and continue processing. The database constraint working as
   designed actually protected us from bad data.

2. Silent commit failures
   Our first attempt at the matching loop used nested savepoints
   inside a batched database connection. SQLAlchemy handled this
   inconsistently across versions and some updates silently never
   committed. We rewrote it to use one transaction per row -
   slower, but clearer and bulletproof.

3. Expanding the manual map mid-phase
   Our first matching run revealed we were missing several major
   public companies - Dell, HP, Micron, Wayfair, Unity, and others.
   The yfinance tier missed them because their short names fell
   below our similarity threshold. Rather than loosening the
   threshold (which would introduce false positives), we expanded
   the manual map from ~60 to ~130 entries, adding the specific
   companies we'd identified as high-impact gaps. This is the right
   engineering workflow - measure, identify specific gaps, fix
   precisely. Don't make the algorithm worse to paper over
   missing data.

What This Enables
-----------------
With 89 companies matched to tickers, Phase 2b can now pull daily
stock prices from Yahoo Finance for each one. Those prices plus our
layoff event dates are everything Phase 3 needs to compute whether
stock prices systematically react to layoff announcements.

What We Can Query Now
---------------------
  -- How many companies did we resolve by method?
  SELECT match_method, COUNT(*) FROM companies GROUP BY match_method;

  -- Find the high-profile companies that got matched
  SELECT company_name, ticker_symbol FROM companies
  WHERE match_method = 'manual' ORDER BY company_name;

  -- How much layoff impact did we capture vs miss?
  SELECT
    CASE WHEN ticker_symbol IS NOT NULL THEN 'matched' ELSE 'missed' END AS status,
    COUNT(DISTINCT c.company_id) AS companies,
    SUM(le.employees_laid_off) AS total_laid_off
  FROM companies c
  LEFT JOIN layoff_events le USING (company_id)
  GROUP BY status;

One Sentence Summary For The Oral Exam
--------------------------------------
"We used a layered entity-resolution strategy - hand-curated
override map, then yfinance validation with fuzzy name verification,
then fall-through to unresolved - and matched 89 companies covering
51% of total layoff impact while deliberately avoiding aggressive
fuzzy matching that would have introduced silent wrong answers."
====================================================================
